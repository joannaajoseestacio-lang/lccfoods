import {LoginForm} from "@/components/login-form"

export default function LoginPage() {
    return (
        <div className="w-full mx-4 md:mx-0 flex h-screen items-center justify-center">
            <LoginForm />
        </div>

    )   
}